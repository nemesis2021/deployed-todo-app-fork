{
  "name": "deployed-to-do-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js $$ npm start",
    "client": "node ./client/webpack.config.js"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
